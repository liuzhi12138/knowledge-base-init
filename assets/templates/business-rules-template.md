# {模块名} 模块业务规则

> 本文件是 **{项目名}** 的 **{模块名}模块** 的业务规则权威文档。
> AI 在处理任何涉及 {模块名} 的业务代码任务前，**必须完整阅读本文件**。
>
> 内容分层：
> - **已确认业务规则**：经团队确认后写入本文
> - **源码事实**：记录在 `{模块目录}/source-facts.md`
> - **历史旁路**：记录在 `{模块目录}/legacy-and-side-paths.md`
> - **治理缺口**：记录在 `{模块目录}/governance-gaps.md`

---

## 零、TL;DR（30 秒速览）

- 核心操作 1 → 走什么入口 / 调什么方法
- 核心操作 2 → 走什么入口 / 调什么方法
- 禁止事项速列

---

## 一、模块禁止事项

1. **禁止 {高危行为1}**：{说明}
2. **禁止 {高危行为2}**：{说明}

---

## 二、核心入口速查

| 维度 | 入口A | 入口B |
|------|-------|-------|
| **核心职责** | {职责描述} | {职责描述} |
| **接口** | `POST /api/{path}` | `POST /api/{path}` |
| **Controller** | `XxxController#method` | `XxxController#method` |
| **Service** | `XxxService#method` | `XxxService#method` |

---

## 三、核心流程详述

### 3.1 流程A

**调用链**：
```
Controller → Service → ... → Repository → Mapper/DAO
```

**入参**：`XxxDTO`，必须包含：
- 关键参数说明

**核心行为**：{步骤说明}

---

## 四、关键数据结构

| 模型 | 路径 | 说明 |
|------|------|------|
| `XxxModel` | `path/to/model` | {说明} |

**关键字段**：

| 字段 | 类型 | 含义 | 变更入口 |
|------|------|------|----------|
| `field1` | type | {含义} | {谁改的} |

---

## 五、关联模块交互

| 关联方 | 方式 | 说明 |
|--------|------|------|
| {下游} | HTTP/RPC/MQ | {用途} |

---

## 六、常见陷阱

| 陷阱 | 表现 | 正确做法 |
|------|------|----------|
| {陷阱} | {错误表现} | {正确做法} |

---

## 七、代码锚点

| 类/文件 | 路径 | 用途 |
|---------|------|------|
| `XxxController` | `path/to/file` | {用途} |
